Before building this animation, read docs/15_MOTION_DESIGN.md and the Motion section of design-system/MASTER.md. Use tokens from lib/motion.ts only. Choose the library by the rules in docs/15 §1 (Motion by default, GSAP only for scroll-scrubbed sections, Lottie/Rive for illustrated assets, lazy-loaded). Animate only transform/opacity/pathLength. Build the reduced-motion version at the same time. Reserve layout space so CLS stays 0. When done, capture Playwright screenshots/video at 360px and 1280px with and without reduced motion, confirm Lighthouse performance is still ≥ 90, then tick the matching MG-/DM- item in pm/PROGRESS.md. Animation request: $ARGUMENTS
