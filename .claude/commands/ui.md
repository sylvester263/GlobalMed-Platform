Before generating this component, read design-system/MASTER.md. Use the 21st.dev Magic MCP to generate it, and include in the Magic prompt: the color tokens, type scale, radius scale, spacing scale and the anti-patterns list from MASTER.md. After generation, refactor the component to use our Tailwind tokens (no hard-coded hex), check keyboard focus and contrast, and add it to the /styleguide page. Component request: $ARGUMENTS
